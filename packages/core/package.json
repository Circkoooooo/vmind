{
	"name": "@vmind/core",
	"version": "0.2.0",
	"description": "",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "rimraf dist && tsc --outDir dist",
		"publish": "npm publish --access public",
		"prepublishOnly": "npm run build && npm run publish"
	},
	"author": "cirko",
	"license": "ISC"
}
